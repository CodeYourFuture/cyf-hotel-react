// import React from "react";
// import { format } from 'date-fns';
// import FakeBookings from "./data/fakeBookings.json";

// const CalcDays = () => {
//     const outDate = format(FakeBookings.push.createdAt, "mm-dd-yyyy");
//     const inDate = format(FakeBookings.push.createdAt, "mm-dd-yyyy");
//     const fake = FakeBookings;
//     return (
//         <div>
//             {fake.map(() => {
//                 // outDate = format(fake.push.createdAt, "mm-dd-yyyy")
//                 // inDate = format(fake.push.createdAt, "mm-dd-yyyy")
//             })}
//         </div>
//     )
// };

// export default CalcDays;
