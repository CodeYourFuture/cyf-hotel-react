import React from "react";

// Render submit button, called by `BookingForm.js`.
const SubmitButton = () => {
  return <button className="btn btn-primary">Make Booking</button>;
};

export default SubmitButton;
