import React from 'react';

  const Counter=(props)=> {

    

    return(
      <div>Results {props.resultsCounter}</div>
    )}

export default Counter;
// Add a counter above the results table
//  which shows the number of results that match the search filter