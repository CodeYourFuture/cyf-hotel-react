import React from "react";

const SearchButton = props => {
  return <button className={props.className}>Search</button>;
};

export default SearchButton;

{
  /* <button className="btn btn-primary">Search</button> */
}
