import React from "react";

const SearchButton = () => {
  return (
    <div id="SearchButton">
      <button className="Button_Search_CSS">Search</button>
    </div>
  );
};

export default SearchButton;
