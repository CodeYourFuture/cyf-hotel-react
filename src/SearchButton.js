import React from "react";

const SearchButton = () => {
  return (
    <input
      type="text"
      id="customerName"
      className="form-control"
      placeholder="Customer name"
    />
  );
};

export default SearchButton;
