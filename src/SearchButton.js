import React from "react";

const SearchButton = () => {
  return (
    <div id="SearchButton">
      <button className="btn btn-primary">Search</button>
    </div>
  );
};

export default SearchButton;
