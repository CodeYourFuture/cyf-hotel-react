import React, { useState } from "react";
import SearchResults from "./SearchResults";

function CustomerProfile(props) {
  return <div>{console.log(props.id)}</div>;
}
export default CustomerProfile;
